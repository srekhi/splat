import React from 'react';
import HeaderContainer from './left_nav/header_container';

//eventually going to have LeftNav, Main, and Detail Component in here only.
const HomeContainer = () => {
  return(
    <main className="left-nav">
      <HeaderContainer />;
   </main>);
};

export default HomeContainer;
